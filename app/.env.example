VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Base URL used for public "copy link" share URLs. Set this so the desktop/native
# app produces the real web URL instead of localhost. Defaults to the current origin.
# VITE_PUBLIC_BASE_URL=https://your-deployed-app.example.com

